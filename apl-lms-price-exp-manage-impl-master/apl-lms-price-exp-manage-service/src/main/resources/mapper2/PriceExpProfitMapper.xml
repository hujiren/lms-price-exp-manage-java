<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apl.lms.price.exp.manage.mapper2.PriceExpProfitMapper">

    <resultMap id="BaseResultMap" type="com.apl.lms.price.exp.pojo.po.PriceExpProfitPo">
        <result property="costProfit" column="cost_profit" typeHandler="com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler"/>
        <result property="increaseProfit" column="increase_profit" typeHandler="com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler"/>
    </resultMap>

    <!--新增-->
    <insert id="addProfit">
        insert into price_exp_profit(
            id,
            increase_profit,
            cost_profit
        )values (
            #{po.id},
            #{po.increaseProfit,typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler},
            #{po.costProfit,typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler}
            )
    </insert>

    <!--更新-->
    <update id="updProfit">
        update price_exp_profit set
            <if test="po.increaseProfit != null">
                increase_profit = #{po.increaseProfit,typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler},
            </if>
            <if test="po.costProfit != null">
                cost_profit = #{po.costProfit,typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler}
            </if>
        where
            id = #{po.id}
    </update>

    <!-- 查找列表 2020-09-11-->
    <select id="getIncreaseProfit" resultMap="BaseResultMap">
        SELECT
           id, 
           increase_profit
        FROM
           price_exp_profit
        where
            id = #{priceId}
    </select>

    <!--批量删除-->
    <delete id="delBatch">
        delete from price_exp_profit where id in(${ids})
    </delete>

    <!--校验id是否存在-->
    <select id="exists" resultType="com.apl.lms.price.exp.pojo.po.PriceExpProfitPo">
        select id, cost_profit from price_exp_profit where id = #{id}
    </select>

    <select id="getPriceProfit" resultType="com.apl.lms.price.exp.pojo.po.PriceExpProfitPo">
        select id, increase_profit, cost_profit from price_exp_profit where id = #{quotePriceId}
    </select>

</mapper>
