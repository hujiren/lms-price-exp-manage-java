<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apl.lms.price.exp.manage.mapper.PriceExpCostMapper">

    <!--根据主表id获取详情-->
    <select id="getPriceExpCostInfo" resultType="com.apl.lms.price.exp.pojo.vo.PriceExpCostVo">
        select
            id,
            partner_id,
            price_status,
            price_code,
            price_name,
            channel_category,
            price_main_id
        from
            price_exp_cost
        where
            id = #{id}
    </select>

    <!--根据主表id获取统计条数-->
    <select id="getPriceDataIdCount" resultType="java.lang.Integer">
        select count(id) from price_exp_cost where price_main_id in
        <foreach collection="ids" separator="," open="(" close=")" item="row" index="">
            #{row}
        </foreach>
    </select>

    <!--获取主表ids-->
    <select id="getPriceDataIds" resultType="com.apl.lms.price.exp.pojo.entity.RelevanceForMainData">
        select main.price_data_id, main.id from price_exp_main main
        left join price_exp_cost cost on cost.price_main_id = main.id where cost.id in
        <foreach collection="ids" close=")" index="" item="row" open="(" separator=",">
            #{row}
        </foreach>
    </select>

    <!--获取主表id-->
    <select id="getPriceDataId" resultType="java.lang.Long">
        select
            main.price_data_id
        from
            price_exp_main main
        left join
            price_exp_cost cost
        on
            main.id = cost.price_main_id
        where
            cost.id = #{id}
    </select>

    <!--组装批量删除条件集合-->
    <select id="getPriceListForDel" resultType="com.apl.lms.price.exp.pojo.entity.PriceListForDelBatch">
        select
            cost.id,
            cost.quote_price_id,
            price_main_id,
            main.price_data_id
        from
            price_exp_cost cost left join price_exp_main main on cost.price_main_id = main.id
        where
            cost.id in
        <foreach collection="ids" separator="," open="(" close=")" item="row" index="">
            #{row}
        </foreach>
    </select>

    <!--批量删除-->
    <delete id="delBatch">
        delete from price_exp_cost where id in(${str})
    </delete>
</mapper>