<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apl.lms.price.exp.manage.mapper.PriceExpProfitMapper">

    <!--新增-->
    <insert id="addProfit">
        insert into price_exp_profit(
            id,
            price_id,
            increase_profit,
            final_profit,
            start_weight
        )values (
            #{po.id},
            #{po.priceId},
            #{po.increaseProfit,typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler},
            #{po.finalProfit,typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler},
            #{po.startWeight}
        )
    </insert>

    <!--更新-->
    <update id="updProfit">
        update price_exp_profit set
            <if test="po.priceId != null">
                price_id = #{po.priceId},
            </if>
            <if test="po.increaseProfit != null">
                increase_profit = #{po.increaseProfit,typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler},
            </if>
            <if test="po.finalProfit != null">
                final_profit = #{po.finalProfit,typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler},
            </if>
            start_weight = #{po.startWeight}
        where
            id = #{po.id}
    </update>

    <!-- 查找列表 2020-09-11-->
    <select id="getList" resultType="com.apl.lms.price.exp.pojo.vo.PriceExpProfitListVo">
        SELECT
           id, 
           price_id,
           increase_profit,
           start_weight
        FROM
           price_exp_profit
        where
            price_id = #{priceId}
    </select>

    <!--批量删除-->
    <delete id="delBatch">
        delete from price_exp_profit where price_id in(${ids})
    </delete>

</mapper>
