<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apl.lms.price.exp.manage.mapper.PriceExpDataMapper">

    <!--根据主表id批量删除-->
    <delete id="deleteBatchById">
        delete from price_exp_data where id in
        <foreach collection="ids" separator="," open="(" close=")" item="row" index="">
            #{row}
        </foreach>
    </delete>

    <!--根据主表id获取详细数据-->
    <select id="getPriceExpDataInfoById" resultType="com.apl.lms.price.exp.pojo.vo.PriceExpDataVo">
        select
            price_data
        from
            price_exp_data
        where
            id = #{id}
    </select>

    <!--新增-->
    <insert id="insertData">
        insert into price_exp_data(id, price_data) values (#{po.id}, #{po.priceData,typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler})
    </insert>

    <!--更新-->
    <update id="updById">
        update price_exp_data set price_data = #{po.priceData,typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler} where id = #{po.id}
    </update>
</mapper>