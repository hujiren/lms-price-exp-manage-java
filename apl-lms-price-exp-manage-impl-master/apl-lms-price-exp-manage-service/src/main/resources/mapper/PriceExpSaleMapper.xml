<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apl.lms.price.exp.manage.mapper.PriceExpSaleMapper">
    <!--新增-->
    <insert id="addPriceExpSale">
        insert into price_exp_sale(
            id,
            price_code,
            price_name,
            customer_groups_id,
            customer_groups_name,
            customer_ids,
            customer_name,
            price_status,
            quote_price_id,
            channel_category,
            price_main_id
        )values(
            #{po.id},
            #{po.priceCode},
            #{po.priceName},
            #{po.customerGroupsId,typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler},
            #{po.customerGroupsName},
            #{po.customerIds,typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler},
            #{po.customerName},
            #{po.priceStatus},
            #{po.quotePriceId},
            #{po.channelCategory},
            #{po.priceMainId}
        )
    </insert>

    <!--根据主表id获取销售价格表Id-->
    <select id="getPriceExpSaleInfoById" resultType="com.apl.lms.price.exp.pojo.vo.PriceExpSaleVo">
        select
            id,
            price_code,
            price_name,
            customer_groups_id,
            customer_groups_name,
            customer_ids,
            customer_name,
            price_status,
            channel_category,
            price_main_id
        from
            price_exp_sale
        where
            id = #{id}
    </select>

    <!--更新-->
    <update id="updateSaleById">
        update price_exp_sale set
        <if test="po.priceCode != null">
            price_code = #{po.priceCode},
        </if>
        <if test="po.customerGroupsId != null">
            customer_groups_id = #{po.customerGroupsId, typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler},
        </if>
        <if test="po.customerGroupsName != null">
            customer_groups_name = #{po.customerGroupsName},
        </if>
        <if test="po.customerIds">
            customer_ids = #{po.customerIds, typeHandler=com.apl.lms.price.exp.lib.typeHandler.CommonJsonbHandler},
        </if>
        <if test="po.customerName != null">
            customer_name = #{po.customerName},
        </if>
        <if test="po.priceStatus != null">
            price_status = #{po.priceStatus},
        </if>
            price_name = #{po.priceName},
            channel_category = #{po.channelCategory}
        where id = #{po.id}
    </update>


    <!--构建批量删除销售价格表条件集合-->
    <select id="getPriceListForDel" resultType="com.apl.lms.price.exp.pojo.bo.PriceListForDelBatchBo">
        select
            sale.id,
            sale.quote_price_id,
            sale.price_main_id,
            main.price_data_id
        from
            price_exp_sale sale left join price_exp_main main on sale.price_main_id = main.id
        where
            sale.id in
        <foreach collection="ids" index="" item="row" close=")" open="(" separator=",">
            #{row}
        </foreach>
    </select>

    <!--批量删除-->
    <delete id="delBatch">
        delete from price_exp_sale where id in(${ids})
    </delete>

    <!--获取主表id-->
    <select id="getMainId" resultType="java.lang.Long">
        select price_main_id from price_exp_sale where id = #{id}
    </select>
</mapper>